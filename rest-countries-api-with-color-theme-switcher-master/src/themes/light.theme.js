import colors from "assets/styles/colors";

const lightTheme = {
  name: "Light",
  id: "T01",
  colors: {
    body: {
      bg: colors.gray.veryLight,
      text: colors.blue.veryDarkText,
    },
    elements: {
      bg: colors.white,
      shadow: "#85858526",
    },
  },
};

export default lightTheme;
