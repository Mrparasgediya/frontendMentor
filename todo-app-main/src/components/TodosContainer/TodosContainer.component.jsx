import React from "react";
import * as S from "./TodosContainer.styles";

function TodosContainer({ children }) {
  return <S.TodosContainer>{children}</S.TodosContainer>;
}

export default TodosContainer;
